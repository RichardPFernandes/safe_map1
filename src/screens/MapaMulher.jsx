import { View } from "react-native";
import { Paragraph } from "react-native-paper";

export const MapaMulher = () => {
  return (
    <View style={{ flex: 1, alignContent: "center", alignItems: "center" }}>
      <Paragraph>Mapa Mulher</Paragraph>
    </View>
  );
};
